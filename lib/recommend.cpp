#include "recommend.hpp"

namespace Recommend {
    string recommend(const string &wrong_command) 
    {
	// calc match score for wrong command
	// for each match entry, add biased match score to get_score(entry)
    }
    
}
